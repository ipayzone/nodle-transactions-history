{
  "name": "nodle-transaction-history",
  "version": "1.0.0",
  "description": "Nodle POS project",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "codegen": "./node_modules/.bin/subql codegen",
    "validate": "./node_modules/.bin/subql validate",
     "start:docker": "docker-compose pull && docker-compose up --remove-orphans"
  },
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Technoprepay",
  "license": "Apache-2.0",
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@polkadot/api": "^7",
    "@subql/cli": "^0.23.0",
    "@subql/types": "^0.12.0",
    "typescript": "^4.7.4"
  },
  "resolutions": {
    "ipfs-unixfs": "6.0.6"
  }
}
