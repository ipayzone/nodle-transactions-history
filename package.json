{
  "name": "nodle-transactions-historyV1.0.0",
  "version": "1.0.0",
  "description": "Nodle POS project",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "start:docker": "docker-compose pull && docker-compose up --remove-orphans",
	"validate": "./node_modules/.bin/subql validate",
    "codegen": "./node_modules/.bin/subql codegen"
  },  
  "homepage": "https://github.com/subquery/subql-starter",
  "repository": "github:subquery/subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Jay Ji",
  "license": "Apache-2.0",
  "dependencies": {
    "@polkadot/util": "8.7.1",
    "@polkadot/util-crypto": "8.7.1",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@polkadot/api": "^7",
    "@subql/cli": "^0.25.0",
    "@subql/types": "^0.14.0",
    "typescript": "^4.7.4"
  },
  "exports": {
    "chaintypes": "src/chaintypes.ts"
  }
}
